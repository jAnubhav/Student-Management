title: UpdateStudentRequest
description: Represents the request body for updating an existing student's details in the student management system. All fields are optional to allow for partial updates.
allOf:
  - $ref: './StudentConfigured.yaml'
  - $ref: './StudentAssigned.yaml#/components/schemas/Status'

example:
  address: "D-46, Sector 39"
  city: "Noida"
  state: "Uttar Pradesh"
  zipcode: "201301"
